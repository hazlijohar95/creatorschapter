
/**
 * If you want to further modularize styling utilities, add here, e.g.:
 * export function getCellClass(selected: boolean, today: boolean) {...}
 * 
 * For now, we're centralizing most styling in the config file,
 * so this file can be expanded in the future as needed.
 */
