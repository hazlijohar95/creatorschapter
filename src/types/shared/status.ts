
/**
 * Common status types used across different features
 */

// Application status types
export type ApplicationStatus = "pending" | "approved" | "rejected" | "in_discussion";

// Campaign status types
export type CampaignStatus = "active" | "draft" | "paused" | "completed";

// Opportunity status types
export type OpportunityStatus = "open" | "pending" | "closed" | "expired";
